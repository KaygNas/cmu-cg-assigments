export const scale = (a: number) => (u: number) => {
  return a * u
}

export const add = (u: number) => (v: number) => {
  return u + v
}

export const subtract = (u: number) => (v: number) => {
  return u - v
}

